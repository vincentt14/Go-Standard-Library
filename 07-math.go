package main

import (
	"fmt"
	"math"
)

// dokumentasi packaage math : https://pkg.go.dev/math

func main7(){
	fmt.Println(math.Ceil(1.60))
	fmt.Println(math.Floor(1.60))
	fmt.Println(math.Round(1.60))
	fmt.Println(math.Max(10, 11))
	fmt.Println(math.Min(10, 11))
}